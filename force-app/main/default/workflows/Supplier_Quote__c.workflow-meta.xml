<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <rules>
        <fullName>Set Supplier Quote Name</fullName>
        <active>true</active>
        <formula>not(isblank( Account__c ))</formula>
        <triggerType>onAllChanges</triggerType>
    </rules>
    <rules>
        <fullName>Set Supplier Quote Name-Contract</fullName>
        <active>true</active>
        <formula>not(isblank( Customer_Contract__c  ))</formula>
        <triggerType>onAllChanges</triggerType>
    </rules>
</Workflow>
